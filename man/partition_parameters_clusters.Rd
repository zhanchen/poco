% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{partition_parameters_clusters}
\alias{partition_parameters_clusters}
\title{Partition parameters into multiple correlated clusters + a remainder}
\usage{
partition_parameters_clusters(
  x,
  threshold = NULL,
  k = NULL,
  min_size = 10L,
  linkage = "average",
  force_remainder = NULL,
  simple_output = TRUE
)
}
\arguments{
\item{x}{A correlation matrix (e.g. from \code{\link[=posterior_correlation]{posterior_correlation()}}).}

\item{threshold}{Numeric in \verb{(0, 1]}; cut the dendrogram at height
\code{1 - threshold}. Mutually exclusive with \code{k}.}

\item{k}{Integer; cut the dendrogram into exactly \code{k} clusters.
Mutually exclusive with \code{threshold}.}

\item{min_size}{Minimum cluster size for a group to be kept as its own
block. Either a \strong{positive integer} (count), or a \strong{numeric proportion
strictly between 0 and 1}, in which case it is converted with
\code{ceiling(min_size * n_parameters)} and an informative message is printed.
For count-style inputs, negative values are converted to their absolute
value and non-integer values are rounded up with \code{ceiling()}, both with
warnings. The effective minimum is then clamped to at least \code{2}; if
conversion or direct input yields \verb{< 2}, it is reset to \code{2} with a warning.
Default \code{10} (integer count).}

\item{linkage}{Linkage method for \code{\link[stats:hclust]{stats::hclust()}}. Default
\code{"average"} (matches what the heatmap uses to reorder).}

\item{force_remainder}{Optional; parameters forced into \strong{\code{remainder}}
(never assigned to \strong{\code{blocks}}), regardless of clustering. \code{NULL}
(default) keeps the partition as cut from the tree. Otherwise supply
a tidy-select expression (interpreted like \code{\link[dplyr:select]{dplyr::select()}} column
semantics), e.g. \code{tidyselect::starts_with("cor_")},
\code{tidyselect::contains("gamma")}, or a \code{c()} / character vector of names.
Names must appear in \code{rownames(x)}.}

\item{simple_output}{Logical. If \code{TRUE} (default), return a \strong{plain}
\code{\link[base:list]{base::list()}} of character vectors only: one element per kept cluster
(names \code{cluster_1}, \code{cluster_2}, ...), same shape as a manual
\strong{\code{partition}} for \code{\link[=compress_posterior]{compress_posterior()}} / \code{\link[=compress_brmsfit]{compress_brmsfit()}}. All
\strong{cluster-related metadata is omitted} (\code{cluster_id}, \code{hclust},
\code{params}, and there is no \code{remainder} component). Parameters \strong{not}
appearing in any list element are still the \strong{remainder} for
blockwise compression: downstream code treats them as unclustered. If
\code{FALSE}, return the full \strong{\code{poco_partition_clusters}} object below
(for \code{\link[=plot_posterior_correlation]{plot_posterior_correlation()}} cluster outlines, printing, and
inspection of \code{remainder} explicitly).}
}
\value{
If \strong{\code{simple_output}} is \code{TRUE}: a plain named \code{list} of
character vectors (\code{cluster_1}, \code{cluster_2}, ...), or \strong{\code{list()}} when
no cluster passes \strong{\code{min_size}} (all parameters are remainder for
\code{\link[=compress_posterior]{compress_posterior()}} / \code{\link[=compress_brmsfit]{compress_brmsfit()}}). If \code{FALSE}, a list
of class \code{"poco_partition_clusters"} with components:
\describe{
\item{\code{blocks}}{named list of character vectors (\code{cluster_1},
\code{cluster_2}, ...), sorted by descending size;}
\item{\code{remainder}}{character vector of parameters not in any kept cluster;}
\item{\code{cluster_id}}{named integer vector mapping every parameter
to its kept-cluster id (1..K) or \code{NA} if it landed in \code{remainder};}
\item{\code{hclust}}{the \code{\link[stats:hclust]{stats::hclust()}} object used for cutting and
for reordering (re-used by \code{\link[=plot_posterior_correlation]{plot_posterior_correlation()}});}
\item{\code{params}}{the rule's parameters (\code{threshold}, \code{k}, \code{min_size}
effective count, optional \code{min_size_proportion} if a proportion was
given, \code{linkage}, optional \code{force_remainder} if any names were forced).}
}
}
\description{
Hierarchical-clustering counterpart to \code{\link[=partition_parameters]{partition_parameters()}}. Builds
clusters from the same \verb{1 - |corr|} distance used by the heatmap, cuts
the dendrogram by either a correlation \strong{\code{threshold}} (cluster members
have average linkage \verb{|corr| >= threshold}) \strong{or} by a target number
of clusters \strong{\code{k}}, and then keeps only clusters whose size is at
least \strong{\code{min_size}}. Parameters not in any kept cluster are collected
in \strong{\code{remainder}} (weakly correlated / small groups / singletons).
}
\details{
Intended use: feed each cluster (block A1, A2, ...) into a richer
(non-diagonal) \code{mclust} family, while keeping \strong{\code{remainder}} on a cheap
diagonal/independent approximation.
}
\examples{
set.seed(3)
n <- 600L
a <- matrix(rnorm(n), n, 3)
a[, 2] <- a[, 1] + 0.05 * rnorm(n)
a[, 3] <- a[, 1] - 0.05 * rnorm(n)
b <- matrix(rnorm(n * 4L), n, 4L)
draws <- cbind(a, b)
colnames(draws) <- c("a1", "a2", "a3", "b1", "b2", "b3", "b4")
cm <- posterior_correlation(draws)
# Default: plain list of clusters (pass-through to compress_* `partition`)
partition_parameters_clusters(cm, threshold = 0.5, min_size = 2L)
# Full metadata (heatmap outlines, `$remainder`, `$hclust`, ...)
partition_parameters_clusters(cm, threshold = 0.5, min_size = 2L,
  simple_output = FALSE)
# Proportion in (0, 1): minimum size = ceiling(0.25 * ncol(cm))
partition_parameters_clusters(cm, threshold = 0.5, min_size = 0.25)
# Keep cor_* / residual correlations in the cheap remainder block:
partition_parameters_clusters(cm, threshold = 0.5, min_size = 2L,
  force_remainder = tidyselect::starts_with("cor_"))
partition_parameters_clusters(cm, threshold = 0.5, min_size = 2L,
  force_remainder = c("a1", "a2"))
}
\seealso{
\code{\link[=posterior_correlation]{posterior_correlation()}}, \code{\link[=partition_parameters]{partition_parameters()}},
\code{\link[=plot_posterior_correlation]{plot_posterior_correlation()}}.
}
