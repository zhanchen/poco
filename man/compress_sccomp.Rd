% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compress_posterior.R
\name{compress_sccomp}
\alias{compress_sccomp}
\title{Compress the posterior of an sccomp fit}
\usage{
compress_sccomp(
  sccomp_obj,
  method = c("mclust", "mvdens_gmm", "mvdens_kde"),
  variables = NULL,
  n_components = 3L,
  model_name = NULL,
  verbose = FALSE,
  remove_csvs = FALSE,
  ...
)
}
\arguments{
\item{sccomp_obj}{An sccomp fit object (with \code{attr(x, "fit")} a
\code{CmdStanMCMC}).}

\item{method}{One of 'mclust', 'mvdens_gmm', 'mvdens_kde'.
See \code{\link[=compression_methods]{compression_methods()}}.}

\item{variables}{Optional character vector of parameter names to keep.
If \code{NULL} (default) all columns are used.}

\item{n_components}{Integer number of mixture components (used by
\code{"mclust"} and \code{"mvdens_gmm"}). Default \code{3}.}

\item{model_name}{\code{mclust} covariance structure (e.g. \code{"VVV"}, \code{"EEE"},
or a vector of allowed model names). Ignored by other methods.
When \code{NULL} (default) \code{poco} auto-selects a sensible set: the
spherical and diagonal models \code{c("EII", "VII", "EEI", "EVI", "VEI", "VVI")} are used when \code{nrow(draws) <= ncol(draws)} so covariances
remain identifiable, otherwise mclust's full default set is used
and BIC picks the best.}

\item{verbose}{Logical; print blockwise progress messages. Default \code{FALSE}
(silent). When \code{TRUE} and blockwise \code{partition} is used: prints whether
cluster blocks run in parallel or sequentially, worker count
(BiocParallel), enables the BiocParallel \strong{progress bar} when supported
(\code{progressbar} on \code{MulticoreParam} / \code{SnowParam}), prints cluster-fit
completion counts in sequential mode, and reports the remainder block.
Backend (\code{mclust} / \code{mvdens}) fitting is kept silent inside blockwise mode.}

\item{remove_csvs}{Logical; if \code{TRUE}, delete the cmdstan CSV files
after compression. Default \code{FALSE}.}

\item{...}{Additional arguments forwarded to the backend (e.g.
\code{\link[mclust:Mclust]{mclust::Mclust()}}).}
}
\value{
A list with \code{compressed} and \code{structure} (the sccomp object
with \code{attr(x, "fit")} cleared). Use \code{\link[=reconstruct_sccomp]{reconstruct_sccomp()}} to rebuild.
}
\description{
Convenience wrapper around \code{\link[=compress_posterior]{compress_posterior()}} for \code{sccomp} fits.
\code{sccomp} stores the underlying \code{cmdstanr} fit in \code{attr(x, "fit")} rather
than \code{x$fit}.
}
\seealso{
\code{\link[=reconstruct_sccomp]{reconstruct_sccomp()}}.
}
