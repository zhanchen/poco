% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{posterior_correlation}
\alias{posterior_correlation}
\title{Compute a posterior correlation or covariance matrix}
\usage{
posterior_correlation(
  draws,
  type = c("correlation", "covariance"),
  variables = NULL,
  method = c("pearson", "spearman", "kendall"),
  use = "everything",
  drop_zero_var = TRUE
)
}
\arguments{
\item{draws}{Draws-like input (see \code{\link[=compress_posterior]{compress_posterior()}}): matrix,
\code{data.frame}, \verb{posterior::draws_*}, 3-D iter x chain x var array,
\code{brms::brmsfit}, \code{rstan::stanfit}, \code{cmdstanr::CmdStanMCMC}, or any
object \code{\link[posterior:draws_matrix]{posterior::as_draws_matrix()}} accepts when the \code{posterior}
package is installed.}

\item{type}{One of \code{"correlation"} (default) or \code{"covariance"}.}

\item{variables}{Optional character vector of parameters to keep.}

\item{method}{Correlation method: one of \code{"pearson"} (default),
\code{"spearman"}, \code{"kendall"}. Forwarded to \code{\link[stats:cor]{stats::cor()}}. Ignored
when \code{type = "covariance"}.}

\item{use}{Handling of missing values; forwarded to
\code{\link[stats:cor]{stats::cor()}} / \code{\link[stats:cor]{stats::cov()}}. Default \code{"everything"}.}

\item{drop_zero_var}{Logical; if \code{TRUE} (default), parameters with
zero posterior SD are dropped before computing correlations (else
\code{cor()} would fill rows/cols with \code{NA}).}
}
\value{
A symmetric numeric matrix with row/col names equal to the
parameter names, and attributes:
\describe{
\item{\code{type}}{\code{"correlation"} or \code{"covariance"}}
\item{\code{method}}{correlation method (or \code{NA_character_} for cov)}
\item{\code{n_draws}}{number of MCMC draws used}
\item{\code{n_params}}{number of parameters in the matrix}
\item{\code{dropped_zero_var}}{names of parameters dropped, if any}
}
}
\description{
Thin wrapper around \code{\link[stats:cor]{stats::cor()}} / \code{\link[stats:cor]{stats::cov()}} that:
}
\details{
\itemize{
\item accepts the same draws-like inputs as \code{\link[=compress_posterior]{compress_posterior()}} (matrix,
data.frame, \verb{posterior::draws_*}, 3-D iter x chain x var array);
\item keeps parameter names on rows/cols;
\item stores a small set of attributes (\code{type}, \code{method}, \code{n_draws},
\code{n_params}) so downstream helpers like \code{\link[=plot_posterior_correlation]{plot_posterior_correlation()}}
and \code{\link[=partition_parameters]{partition_parameters()}} can self-describe their plots/output.
}

This is intended as the first step of building a hybrid compression
that keeps within-component covariance only for the most strongly
correlated parameters.
}
\examples{
set.seed(1)
draws <- matrix(rnorm(500 * 4), ncol = 4,
                dimnames = list(NULL, c("a", "b", "c", "d")))
draws[, "b"] <- draws[, "a"] + 0.05 * rnorm(500)  # induce strong corr
cm <- posterior_correlation(draws)
round(cm, 2)
}
\seealso{
\code{\link[=plot_posterior_correlation]{plot_posterior_correlation()}}, \code{\link[=partition_parameters]{partition_parameters()}}.
}
