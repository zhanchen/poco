% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{plot_posterior_correlation}
\alias{plot_posterior_correlation}
\title{Diagnostic plots of a posterior correlation matrix}
\usage{
plot_posterior_correlation(
  x,
  which = c("heatmap", "hist", "max_abs"),
  absolute = TRUE,
  threshold = NULL,
  max_params_heatmap = 200L,
  dendrogram = c("none", "both", "row", "column"),
  partition = NULL,
  show_cluster_labels = NULL,
  show_cluster_legend = NULL,
  ...
)
}
\arguments{
\item{x}{A matrix from \code{\link[=posterior_correlation]{posterior_correlation()}} (or any symmetric
numeric matrix; rows/cols are treated as parameters).}

\item{which}{One or more of \code{"heatmap"}, \code{"hist"}, \code{"max_abs"}. The
default returns all three.}

\item{absolute}{Logical; if \code{TRUE} (default for \code{"hist"} and
\code{"max_abs"}) summarise on the absolute scale, since both signs of
correlation are equally informative for partitioning.}

\item{threshold}{Optional numeric reference threshold(s) drawn on the
\code{"hist"} and \code{"max_abs"} panels (e.g. \code{0.3}).}

\item{max_params_heatmap}{Integer; if the matrix has more parameters
than this, the heatmap is restricted to the top-\code{max_params_heatmap}
parameters by \verb{max_\{j != i\} |corr(i, j)|}. Avoids unreadable
1000x1000 tiles. Default \code{200}.}

\item{dendrogram}{One of \code{"none"} (default), \code{"both"}, \code{"row"}, or
\code{"column"}. Adds dendrograms next to the heatmap (return value becomes
a \code{patchwork} object when not \code{"none"}). Requires the \code{ggdendro} and
\code{patchwork} packages (Suggests); silently falls back to a plain heatmap
if either is missing.}

\item{partition}{Optional \code{\link[=partition_parameters_clusters]{partition_parameters_clusters()}} result
(\strong{\code{simple_output = TRUE}}: plain list of cluster vectors; or
\strong{\code{simple_output = FALSE}}: a \code{poco_partition_clusters} object), or any
list with the same fields as the full object (\code{cluster_id}, \code{blocks},
\code{remainder}, ...). For \strong{simple} lists (no \code{cluster_id}), cluster
outlines are drawn by mapping block membership to the \strong{visible}
heatmap columns (after any \code{max_params_heatmap} subset and dendrogram
reorder). Parameters not in any block are treated as remainder for the
annotation subtitle.}

\item{show_cluster_labels}{Logical or \code{NULL} (default). If \code{NULL}, on-plot
cluster \strong{text} is omitted when there are many clusters or the heatmap
is dense (few parameters per block); outlines are always drawn. Use
\code{TRUE} / \code{FALSE} to override.}

\item{show_cluster_legend}{Logical or \code{NULL} (default). If \code{NULL}, the
cluster \strong{colour legend} follows similar heuristics (slightly more
permissive than labels). Use \code{TRUE} / \code{FALSE} to override.}

\item{...}{Currently unused.}
}
\value{
A named list of \code{ggplot} objects (one entry per \code{which}).
When called for a single panel, returns the single \code{ggplot}.
}
\description{
Produces lightweight summary plots that help pick a threshold (or
top-N) for \code{\link[=partition_parameters]{partition_parameters()}}. Three views are available:
}
\details{
\describe{
\item{\code{"heatmap"}}{tile plot of the correlation matrix; ordered by
hierarchical clustering on \verb{1 - |corr|} for visual block structure;}
\item{\code{"hist"}}{histogram of off-diagonal values (or their absolute
value if \code{absolute = TRUE}), useful for choosing a threshold;}
\item{\code{"max_abs"}}{sorted curve of \verb{max_\{j != i\} |corr(i, j)|} per
parameter, with optional reference lines at proposed thresholds.}
}

Requires \code{ggplot2} (Suggests). Returns \code{ggplot} objects rather than
drawing, so callers can compose / save them.
}
\seealso{
\code{\link[=posterior_correlation]{posterior_correlation()}}, \code{\link[=partition_parameters]{partition_parameters()}}.
}
