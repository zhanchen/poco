% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{strip_brmsfit_envs}
\alias{strip_brmsfit_envs}
\title{Clear captured environments from a \code{brmsfit}}
\usage{
strip_brmsfit_envs(x)
}
\arguments{
\item{x}{A \code{\link[brms:brmsfit-class]{brms::brmsfit}} object.}
}
\value{
\code{x}, invisibly, with formula environments cleared.
}
\description{
\code{brms} attaches the calling environment to formula (and related) objects.
When \code{brm()} is run inside \code{purrr::map()} or magrittr pipes, that
environment can retain upstream objects (\code{bterms}, \code{bframe}, pipe
placeholders, the fit object itself, etc.) and inflate serialized size
dramatically even though posterior draws are small.
}
\details{
This helper recursively clears those environments by setting them to
\code{NULL} (including \code{terms} objects' \code{.Environment} attribute). The
\code{brmsfit} class and posterior draws are unchanged.

Apply directly to one fit, not with \code{lapply(fit, ...)} (which walks slots
and returns a plain list):

\if{html}{\out{<div class="sourceCode r">}}\preformatted{fit_clean <- strip_brmsfit_envs(fit)
# list-column:
df$brms_fit <- lapply(df$brms_fit, strip_brmsfit_envs)
}\if{html}{\out{</div>}}
}
