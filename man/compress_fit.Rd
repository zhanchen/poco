% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compress_posterior.R
\name{compress_fit}
\alias{compress_fit}
\title{Compress the posterior of a cmdstanr fit}
\usage{
compress_fit(
  fit,
  method = c("mclust", "mvdens_gmm", "mvdens_kde"),
  variables = NULL,
  n_components = 3L,
  model_name = "VVV",
  verbose = FALSE,
  remove_csvs = FALSE,
  ...
)
}
\arguments{
\item{fit}{Either a \code{CmdStanMCMC} object (from \code{cmdstanr::sample()}) or
a character vector of cmdstan CSV file paths.}

\item{method}{One of 'mclust', 'mvdens_gmm', 'mvdens_kde'.
See \code{\link[=compression_methods]{compression_methods()}}.}

\item{variables}{Optional character vector of parameter names to keep.
If \code{NULL} (default) all columns are used.}

\item{n_components}{Integer number of mixture components (used by
\code{"mclust"} and \code{"mvdens_gmm"}). Default \code{3}.}

\item{model_name}{\code{mclust} covariance structure (e.g. \code{"VVV"}, \code{"EEE"}).
Ignored by other methods. Default \code{"VVV"}.}

\item{verbose}{Logical; print backend progress. Default \code{FALSE}.}

\item{remove_csvs}{Logical; if \code{TRUE} and \code{fit} is a CmdStanMCMC,
delete the CSV output files after compression. Default \code{FALSE}.}

\item{...}{Additional arguments forwarded to the backend (e.g.
\code{\link[mclust:Mclust]{mclust::Mclust()}}).}
}
\value{
A \code{posterior_compressed} object (see \code{\link[=compress_posterior]{compress_posterior()}}).
}
\description{
Convenience wrapper around \code{\link[=compress_posterior]{compress_posterior()}} that accepts either a
\code{cmdstanr::CmdStanMCMC} fit or a vector of cmdstan CSV file paths.
}
\examples{
\dontrun{
fit <- mod$sample(data = stan_data, chains = 4)
comp <- compress_fit(fit, method = "mclust", n_components = 5)
samples <- sample_posterior(comp, n_draws = 1000)
}

}
\seealso{
\code{\link[=compress_posterior]{compress_posterior()}}, \code{\link[=compress_brmsfit]{compress_brmsfit()}},
\code{\link[=reconstruct_brmsfit]{reconstruct_brmsfit()}}.
}
