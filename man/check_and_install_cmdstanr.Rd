% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmdstan-utils.R
\name{check_and_install_cmdstanr}
\alias{check_and_install_cmdstanr}
\title{Check (and optionally install) \code{cmdstanr} and CmdStan}
\usage{
check_and_install_cmdstanr()
}
\value{
Invisibly \code{TRUE} if everything is available, \code{FALSE} otherwise.
}
\description{
Light wrapper around the same logic used by \code{sccomp}. Verifies that
\code{cmdstanr} (>= 0.9.0) and a working CmdStan install are available; if not,
it offers an actionable message (and, in non-interactive sessions, attempts
to install \code{cmdstanr} from the Stan-dev R-Universe).
}
\examples{
\dontrun{
check_and_install_cmdstanr()
}
}
