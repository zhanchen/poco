% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/method-blockwise.R
\name{partition_blocks}
\alias{partition_blocks}
\title{Declare manual correlation clusters for blockwise compression}
\usage{
partition_blocks(...)
}
\arguments{
\item{...}{One or more block specifications: a character vector of names,
a \code{list()} of character vectors, a \strong{formula} with tidy-select on the
RHS (e.g. \code{~ tidyselect::starts_with("b_")}), or any expression
understood by \code{\link[tidyselect:eval_select]{tidyselect::eval_select()}} (including bare
\code{tidyselect::starts_with("x")} when captured here).}
}
\value{
An object of class \code{"poco_partition_block_spec"} (a list of
quosures). Normally passed directly to \verb{compress_*(..., partition = )};
resolution against actual parameter names happens during compression.
}
\description{
Captures block specifications with \code{\link[rlang:enquo]{rlang::enquos()}} so you can mix
literal parameter names and \strong{tidy-select} expressions (same semantics as
column selection, e.g. \code{\link[tidyselect:starts_with]{tidyselect::starts_with()}}, \code{\link[tidyselect:starts_with]{tidyselect::contains()}}).
Parameters matched by no block are placed in \strong{\code{remainder}} when you pass
the result as \code{partition} to \code{\link[=compress_posterior]{compress_posterior()}} or \code{\link[=compress_brmsfit]{compress_brmsfit()}}.
}
\details{
For \strong{character-only} blocks you can instead pass a plain \code{list()} of
character vectors; that form is coerced automatically.

\strong{Tidy-select} helpers must be captured: use \code{partition_blocks(...)} or,
in a plain list, a \strong{formula} on the RHS such as
\code{~ tidyselect::starts_with("b_")}. Writing \code{list(tidyselect::starts_with("b_"))}
does \strong{not} work, because R evaluates \code{starts_with()} before compression
and the result is not a column selection expression.

Overlap is resolved \strong{in order}: a parameter is assigned to the first
block that matches it; later blocks do not steal names.

\strong{Singleton blocks} (only one parameter after resolution) are not used as
correlation clusters: those parameters are moved to \strong{\code{remainder}} with a
\code{\link[=warning]{warning()}}, because within-block correlations are undefined for a single
column.
}
\examples{
\dontrun{
draws <- matrix(rnorm(100 * 5), ncol = 5)
colnames(draws) <- c("a1", "b_1", "b_2", "dataset_id_x", "z")
compress_posterior(
  draws,
  method = "mclust",
  n_components = 2L,
  partition = partition_blocks(
    c("a1", "z"),
    tidyselect::starts_with("b_"),
    tidyselect::contains("dataset_id")
  )
)
# Plain list of name vectors only:
compress_posterior(draws, method = "mclust", partition = list(c("a1"), c("b_1", "b_2")))
}
}
\seealso{
\code{\link[=compress_posterior]{compress_posterior()}}, \code{\link[=compress_brmsfit]{compress_brmsfit()}},
\code{\link[=partition_parameters_clusters]{partition_parameters_clusters()}} (default \strong{\code{simple_output = TRUE}}
plain list, or \strong{\code{simple_output = FALSE}} for the full object).
}
