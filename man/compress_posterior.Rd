% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compress_posterior.R
\name{compress_posterior}
\alias{compress_posterior}
\title{Compress a posterior draws matrix}
\usage{
compress_posterior(
  draws,
  method = c("mclust", "mvdens_gmm", "mvdens_kde"),
  variables = NULL,
  n_components = 3L,
  model_name = "VVV",
  verbose = FALSE,
  ...
)
}
\arguments{
\item{draws}{An object containing posterior draws. Accepted:
\itemize{
\item a numeric matrix (rows = draws, cols = parameters);
\item a \code{data.frame} of draws (one column per parameter);
\item any object the \code{posterior} package can convert with
\code{posterior::as_draws_matrix()} (\code{draws_matrix}, \code{draws_array}, ...);
\item a 3-D array of shape \verb{iter x chain x var}.
}}

\item{method}{One of 'mclust', 'mvdens_gmm', 'mvdens_kde'.
See \code{\link[=compression_methods]{compression_methods()}}.}

\item{variables}{Optional character vector of parameter names to keep.
If \code{NULL} (default) all columns are used.}

\item{n_components}{Integer number of mixture components (used by
\code{"mclust"} and \code{"mvdens_gmm"}). Default \code{3}.}

\item{model_name}{\code{mclust} covariance structure (e.g. \code{"VVV"}, \code{"EEE"}).
Ignored by other methods. Default \code{"VVV"}.}

\item{verbose}{Logical; print backend progress. Default \code{FALSE}.}

\item{...}{Additional arguments forwarded to the backend (e.g.
\code{\link[mclust:Mclust]{mclust::Mclust()}}).}
}
\value{
An S3 list of class \code{c("posterior_compressed_<method>", "posterior_compressed", "list")} containing the parameters of the
fitted approximation.
}
\description{
Fits a compact density approximation to a matrix of MCMC draws and
returns an object of class \code{posterior_compressed}. The compressed
object can be sampled from via \code{\link[=sample_posterior]{sample_posterior()}} and evaluated via
\code{\link[=density_posterior]{density_posterior()}}.
}
\examples{
set.seed(1)
draws <- matrix(rnorm(2000 * 3), ncol = 3,
                dimnames = list(NULL, c("alpha", "beta", "sigma")))
comp <- compress_posterior(draws, method = "mclust", n_components = 2)
comp
new_samples <- sample_posterior(comp, n_draws = 500)
dim(new_samples)

}
\seealso{
\code{\link[=compress_fit]{compress_fit()}}, \code{\link[=compress_brmsfit]{compress_brmsfit()}}, \code{\link[=compress_sccomp]{compress_sccomp()}},
\code{\link[=sample_posterior]{sample_posterior()}}, \code{\link[=density_posterior]{density_posterior()}}.
}
