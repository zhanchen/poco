% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{partition_parameters}
\alias{partition_parameters}
\title{Partition parameters into a "correlated" block A and a weaker block B}
\usage{
partition_parameters(
  x,
  rule = c("threshold", "top_n", "top_prop", "min_degree"),
  threshold = NULL,
  n = NULL,
  prop = NULL,
  min_degree = NULL
)
}
\arguments{
\item{x}{A matrix from \code{\link[=posterior_correlation]{posterior_correlation()}} (or any square
correlation matrix).}

\item{rule}{One of:
\describe{
\item{\code{"threshold"}}{block A = parameters whose
\verb{max_\{j != i\} |corr(i, j)| >= threshold}. Requires \code{threshold}.}
\item{\code{"top_n"}}{block A = the \code{n} parameters with the largest
max-off-diagonal \verb{|corr|}. Requires \code{n}.}
\item{\code{"top_prop"}}{block A = the top \code{prop} (e.g. \code{0.1} for the
top 10\%) of parameters by max-off-diagonal \verb{|corr|}. Requires
\code{prop} in \verb{(0, 1]}.}
\item{\code{"min_degree"}}{block A = parameters with at least \code{min_degree}
neighbours whose \verb{|corr| >= threshold} (graph-degree rule).
Requires both \code{threshold} and \code{min_degree}.}
}}

\item{threshold}{Numeric; required for \code{"threshold"} and
\code{"min_degree"}.}

\item{n}{Integer; required for \code{"top_n"}.}

\item{prop}{Numeric in \verb{(0, 1]}; required for \code{"top_prop"}.}

\item{min_degree}{Integer; required for \code{"min_degree"}.}
}
\value{
A list of class \code{"poco_partition"} with components:
\describe{
\item{\code{block_a}}{character vector of parameter names assigned to A;}
\item{\code{block_b}}{character vector of parameter names assigned to B;}
\item{\code{abs_max_offdiag}}{named numeric vector of per-parameter
max-off-diagonal \verb{|corr|} (same names as \code{x}'s rows);}
\item{\code{rule}}{the rule used;}
\item{\code{params}}{a named list of the rule's parameters
(\code{threshold}, \code{n}, \code{prop}, \code{min_degree}).}
}
}
\description{
Given a correlation matrix (e.g. from \code{\link[=posterior_correlation]{posterior_correlation()}}),
classify each parameter as belonging to \strong{block A} (strongly
correlated with at least one other parameter) or \strong{block B}
(everything else) using one of several explicit rules.
}
\details{
The intended use is in a hybrid compression: fit a richer
(non-diagonal) mixture on \strong{block A}, and keep a cheap
diagonal/independent approximation on \strong{block B}.
}
\examples{
set.seed(2)
draws <- matrix(rnorm(500 * 5), ncol = 5,
                dimnames = list(NULL, letters[1:5]))
draws[, "b"] <- draws[, "a"] + 0.05 * rnorm(500)
draws[, "c"] <- draws[, "a"] - 0.05 * rnorm(500)
cm <- posterior_correlation(draws)
partition_parameters(cm, rule = "threshold", threshold = 0.3)
}
\seealso{
\code{\link[=posterior_correlation]{posterior_correlation()}}, \code{\link[=plot_posterior_correlation]{plot_posterior_correlation()}}.
}
