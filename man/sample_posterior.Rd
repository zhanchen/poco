% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_density.R
\name{sample_posterior}
\alias{sample_posterior}
\title{Sample from a compressed posterior}
\usage{
sample_posterior(object, n_draws = NULL, ...)
}
\arguments{
\item{object}{A \code{posterior_compressed} object, or a path to an \code{.rds}
file containing one (a single string).}

\item{n_draws}{Integer number of samples to draw. If \code{NULL} (default),
uses the original number of MCMC draws.}

\item{...}{Currently unused.}
}
\value{
A numeric matrix of draws (rows = draws, cols = parameters).
}
\description{
Draws fresh posterior samples from an object returned by
\code{\link[=compress_posterior]{compress_posterior()}} or its wrappers.
}
\examples{
set.seed(1)
draws <- matrix(rnorm(2000 * 2), ncol = 2,
                dimnames = list(NULL, c("a", "b")))
comp <- compress_posterior(draws, method = "mclust", n_components = 2)
s <- sample_posterior(comp, n_draws = 500)
dim(s)
}
